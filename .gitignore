# Dependencies
node_modules/

# Build output
dist/

# Environment (never commit real Firebase config values you consider private)
.env
.env.local
.env.*.local

# Firebase
.firebase/
firebase-debug.log
firestore-debug.log

# Firebase Admin SDK service-account keys — NEVER commit these (secrets).
# The .example.json sample is safe and intentionally tracked.
serviceAccountKey.json
serviceAccount*.json
!serviceAccountKey.example.json
*firebase-adminsdk*.json
*-service-account*.json

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
